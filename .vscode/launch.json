{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node-terminal",
      "request": "launch",
      "name": "Tauri Dev",
      "command": "npm run tauri dev",
      "cwd": "${workspaceFolder}"
    }
  ]
}